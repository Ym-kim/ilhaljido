// 세션 쿠키 수명 상한 (초). 활성 사용자는 요청마다 리프레시 토큰이 롤링 갱신되므로
// 이 값은 실질적으로 '비활성 N일 후 자동 로그아웃' 기준이 된다.
// Supabase 기본값(~400일)은 사실상 무기한이라 공용 PC·장기 방치 시 위험 → 30일로 단축.
export const SESSION_MAX_AGE = 60 * 60 * 24 * 30 // 30일
